import { FiltroLogsPipe } from './filtro-logs.pipe';

describe('FiltroLogsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroLogsPipe();
    expect(pipe).toBeTruthy();
  });
});
