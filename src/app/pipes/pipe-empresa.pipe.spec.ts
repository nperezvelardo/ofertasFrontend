import { PipeEmpresaPipe } from './pipe-empresa.pipe';

describe('PipeEmpresaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeEmpresaPipe();
    expect(pipe).toBeTruthy();
  });
});
