import { PipePerfilPipe } from './pipe-perfil.pipe';

describe('PipePerfilPipe', () => {
  it('create an instance', () => {
    const pipe = new PipePerfilPipe();
    expect(pipe).toBeTruthy();
  });
});
